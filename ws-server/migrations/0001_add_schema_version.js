/**
 * @file migrations/0001_add_schema_version.js
 * @brief Database schema migration step.
 */

'use strict';

module.exports = {
    async up(pool) {
        console.log('Baseline migration: Database initialized.');
    }
};
